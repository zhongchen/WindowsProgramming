#pragma once
#include <condition_variable>
#include <mutex>
#include <queue>

namespace clock18
{

// The blocking queue is used in the consumer-producer scenario.
// The push, pop, size should be thread-safe.
class BlockingQueue
{
public:

private:

};

}